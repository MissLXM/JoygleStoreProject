server:
  port: 9009
spring:
  application:
    # 服务名称
    name: static-service
  cloud:
    # 注册中心地址
    nacos:
      config:
        server-addr: 124.222.207.217:8848
        group: DEFAULT_GROUP
        ext-config:
          - data-id: static-dev.yml
            refresh: true
            group: DEFAULT_GROUP
      discovery:
        server-addr: 124.222.207.217:8848
